export const environment = {
    production: true,
    apiUrl: 'https://api.prod.example.com', // URL del endpoint de producción
    apiKey: 'your-production-api-key'       // API key de producción
  };
  